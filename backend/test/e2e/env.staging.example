# Staging — copy to env.staging and fill in values
E2E_BASE_URL=https://notify-staging.example.com
E2E_API_KEY=

# Staging typically uses real SMTP — skip Mailpit delivery validation
E2E_SKIP_DELIVERY_VALIDATION=true
